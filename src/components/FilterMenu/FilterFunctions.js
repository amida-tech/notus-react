export const setPercentageRange = (displayData) => displayData;

export const setDomainsOfCare = (displayData) => displayData;

export const setStarRating = (displayData) => displayData;

export const setNCQAAccreditationBonus = (displayData) => displayData;

// Coming soon.
export const setphases = (displayData) => displayData;

// Coming soon.
export const setSubMeasures = (displayData) => displayData;
